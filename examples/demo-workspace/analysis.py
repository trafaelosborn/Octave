"""Small runnable file for exercising Octave's process log."""

context_files = ["paper.tex", "notes.md", "references.bib"]
print(f"Example workspace contains {len(context_files)} deliberate context files.")
